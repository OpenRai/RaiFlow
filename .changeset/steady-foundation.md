---
'@openrai/model': major
'@openrai/webhook': major
'@openrai/raiflow-sdk': major
---

Establish the v3 runtime contract: canonical `/v1` routes, mandatory mutation idempotency, monotonic event cursors and streaming, typed SDK failures, and raw-body webhook signatures with corrected retry policy.
