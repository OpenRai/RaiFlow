---
"@openrai/runtime": minor
---

Add version string to dashboard footer, request detail modal, and rename 'showInternal' filter. Dashboard footer now shows the git describe version. Each requests table row has an info button that opens a modal with the full log entry. Filter label changed from "Include Dashboard Requests" to "Show internal".