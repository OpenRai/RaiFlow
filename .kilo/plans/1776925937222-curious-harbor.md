# Plan: Add Optional Custody Support & Rename Env Vars

## Goal

Add optional custody support to the Docker default config, and rename `RAIFLOW_SEED` / `RAIFLOW_REPRESENTATIVE` to `RAIFLOW_CUSTODY_SEED` / `RAIFLOW_CUSTODY_REP` for clarity across code, examples, and docs.

## Changes

### 1. Add custody section to Docker config

**`packages/runtime/docker/raiflow.yml`** — add commented-out custody section:

```yaml
# custody:
#   seed: "env:RAIFLOW_CUSTODY_SEED"
#   representative: "env:RAIFLOW_CUSTODY_REP"
```

### 2. Rename env vars in examples/docs

| File | Old | New |
|------|-----|-----|
| `raiflow.yml.example` | `RAIFLOW_SEED` | `RAIFLOW_CUSTODY_SEED` |
| `raiflow.yml.example` | `RAIFLOW_REPRESENTATIVE` | `RAIFLOW_CUSTODY_REP` |
| `docker-compose.yml` | `RAIFLOW_SEED` | `RAIFLOW_CUSTODY_SEED` |
| `docker-compose.yml` | `RAIFLOW_REPRESENTATIVE` | `RAIFLOW_CUSTODY_REP` |
| `README.md` | `RAIFLOW_SEED` | `RAIFLOW_CUSTODY_SEED` (3 places) |
| `README.md` | `RAIFLOW_REPRESENTATIVE` | `RAIFLOW_CUSTODY_REP` (3 places) |
| `apps/site/docs/runtime/index.md` | `RAIFLOW_SEED` | `RAIFLOW_CUSTODY_SEED` |
| `apps/site/docs/runtime/index.md` | `RAIFLOW_REPRESENTATIVE` | `RAIFLOW_CUSTODY_REP` |
| `docs/architectural-review.md` | `RAIFLOW_SEED` | `RAIFLOW_CUSTODY_SEED` |

### 3. Update plan file reference

**`.kilo/plans/1776921165193-eager-cactus.md`** — update the 3 references (these are internal planning notes, not user-facing).

## File Inventory

| File | Action |
|------|--------|
| `packages/runtime/docker/raiflow.yml` | Add custody section |
| `raiflow.yml.example` | Rename 2 env vars |
| `docker-compose.yml` | Rename 2 env vars |
| `README.md` | Rename 6 env vars |
| `apps/site/docs/runtime/index.md` | Rename 2 env vars |
| `docs/architectural-review.md` | Rename 1 env var |
| `.kilo/plans/1776921165193-eager-cactus.md` | Rename 3 env vars (plan artifact) |

## Notes

- No code changes needed — the config system already supports `env:NANO_*` pattern, just the variable names are changing
- This is purely a rename + adding the commented custody block to Docker default
- All tests should still pass since no runtime behavior changes