# Images

This directory is the repo location for README and docs imagery.

Reserved asset:

- `adnan-before-after-raiflow.jpg` — the "Before / After" camel graphic showing raw Nano JSON-RPC versus the RaiFlow runtime layer

Adnan (عدنان) is the Nano camel in that image.
